/**
 * Provides the classes necessary to convert latitude-longitude coordinates into a country name, because the crop
 * price API and currency conversion API are indexed by country name.
 */
package textfarming.datasources.coordstocountry;
